<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.armagefinder.morriss.armagefinder" >

    <application
        android:debuggable="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PitcherActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/title_activity_pitcher"
            android:screenOrientation="portrait"
            android:theme="@style/PitcherBackground">
        </activity>
        <activity
            android:name=".ResultsActivity"
            android:label="@string/title_activity_results" >
        </activity>
        <activity
            android:name=".PositionPlayerActivity"
            android:label="@string/title_activity_position_player"
            android:theme="@style/PositionPlayerBackground">
        </activity>
        <activity
            android:name=".DualPlayerActivity"
            android:label="@string/title_activity_dual_player"
            android:theme="@style/DualPlayerBackground">
        </activity>
    </application>

</manifest>
